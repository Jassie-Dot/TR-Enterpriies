{
  "name": "tr-enterprises-premium-fullstack",
  "version": "2.0.0",
  "private": true,
  "description": "Premium full-stack Node.js and Tailwind website for TR-Enterpriies.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css --minify",
    "build:js": "terser public/app.js -o public/app.min.js -c -m",
    "build": "npm run build:css && npm run build:js",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css --watch",
    "check": "node --check server.js && node --check api/index.js && node --check public/app.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "terser": "^5.48.0"
  }
}
